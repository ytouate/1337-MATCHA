import "./globals.css";
import React from 'react';
import { AppProps } from 'next/app';

// TODO: setup apollo client
// TODO: setup moution
// TODO: load poppins hhhhhh font

const Matcha = ({ Component, pageProps }: AppProps) => {
  return <Component {...pageProps} />;
};

export default Matcha;
